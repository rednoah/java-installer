#!/bin/sh

# set working dir
cd "$SYNOPKG_PKGDEST"

. "$SYNOPKG_PKGDEST/install-jdk.sh" 2>&1 | tee "$SYNOPKG_PKGDEST/install-jdk.log"

java -version 2>&1 | tee -a "$SYNOPKG_PKGDEST/install-jdk.log" "$SYNOPKG_TEMP_LOGFILE"

# return successfully
exit 0
